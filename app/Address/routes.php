<?php
/*
Route::group([
    'namespace' => "Address\\Controllers",
  //  'middleware' => ['web'],
        ], function() {
    Route::get('address-fromcep/{cep}', "AddressesController@getFromCep");
    Route::resource('address', "AddressesController");
});

Route::group([
    'namespace' => "Address\\Controllers",
    'middleware' => ['api'],
        ], function() {
    Route::get('api/address-fromcep/{cep}', "AddressesController@getFromCep");
});
*/
// Dados do evento
    // ajustar formulário para excluir bebes e pais
    // Lógica
    //  Carregar fumulário: verifica se há dados cadastrados e exibe no formulário se houver
    //  Enviar formulário: verifica se exite os dados cadstrados, atualiza se houver; insere se não houver; apaga se o dado não for reenviado.
    //  Validação: o prenchimento dos dados não é obrigatório; se o cliente enviar o formulário em branco, os dados são completamnte deletados. 
    // 

// Dados do Página
    // Título, texto e URI
    //  Validação: o prenchimento dos dados não é obrigatório; 
    //              se o cliente enviar o formulário em branco, os dados são completamnte deletados;
    //              a página não fica on-line se os dados não forem preenchidos 
    //              ou usa-se apenas os dados disponíveis para mostrar a página. 
    
// Dados do Convite
    // Título, texto e URI
    //  Validação: o prenchimento dos dados não é obrigatório; 
    //              se o cliente enviar o formulário em branco, os dados são completamnte deletados;
    //              o Convite não fica on-line se os dados não forem preenchidos 
    //              ou usa-se apenas os dados disponíveis para mostrar a página.
    
